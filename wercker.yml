box: alanbem/wercker-php
build:
  steps:
    - script:
        name: echo PHP information
        code: |-
            php -v
            php -i
            composer install && composer update
            ./phpunit-4.8.5.phar tests